<h1 class="center-align">Children</h1>

<table class="striped">
  <thead>
    <tr>
      <th></th>
      <th>Name</th>
      <th>Age</th>
      <th>Gender</th>
      <th>Child Id</th>
      <th>Parents</th>
      <th>Parents Id</th>
    </tr>
  </thead>
  <tbody>
    <% @children.each_with_index do |child,i| %>
      <tr>
        <td><%= i+1 %></td>
        <td><%= child.name %></td>
        <td><%= child.age %></td>
        <td><%= child.gender %></td>
        <td><%= child.id %></td>
        <td><%= @person.find(" #{child.person_id}").name %></td>
        <td><%= @person.find("#{child.person_id}").id %></td>
      </tr>
    <% end %>
  </tbody>
</table>
